[project]
name = "descope-bigquery-mcp"
version = "0.1.0"
description = "Internal MCP server for BigQuery with Descope identity, scopes, and Connection-backed credentials"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=44.0.0",
    "descope-mcp>=0.1.0",
    "fastmcp>=3.2.0",
]
