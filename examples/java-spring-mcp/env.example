# Descope Configuration
# Your Descope Project ID (required)
DESCOPE_PROJECT_ID=your-project-id-here

# Your Descope Management Key (required for outbound app operations)
DESCOPE_MANAGEMENT_KEY=your-management-key-here

# Optional: Custom Descope base URL (defaults to https://api.descope.com)
# Only change this if you're using a custom Descope instance
# DESCOPE_BASE_URL=https://api.descope.com

# Optional: Logging level for Descope SDK
# DESCOPE_LOG_LEVEL=INFO

# Optional: Timeout settings (in milliseconds)
# DESCOPE_TIMEOUT=30000

# Google Calendar Outbound App Configuration
# The app ID for Google Calendar outbound app (this is hardcoded)
# GOOGLE_OUTBOUND_APP_ID=google-calendar

# MCP Server Configuration
# Port for the MCP server (default: 8080)
# MCP_SERVER_PORT=8080

# Optional: Host for the MCP server (default: localhost)
# MCP_SERVER_HOST=localhost
