[project]
name = "fastmcp-tool-scopes"
version = "0.1.0"
description = "FastMCP 3 server with Descope auth and tool-level OAuth scopes"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=3.4.2",
    "python-dotenv>=1.1.1",
    "uvicorn>=0.34.0",
    "starlette>=0.47.1",
]
